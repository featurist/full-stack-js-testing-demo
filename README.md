Demo [browser-monkey](https://github.com/featurist/browser-monkey) testing library.

Clone this and `npm install`.

Run tests in Chrome with `npm test`

Run tests in node (via virtual dom) with `npm run vdom-test`
